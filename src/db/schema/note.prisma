model Note {
    idNote String @id @default(cuid())
    familyId String
    creatorId String
    name String
    desc String

    createdAt DateTime @default(now())

    user User @relation(fields: [creatorId], references: [idUser])
    family Family @relation(fields: [familyId], references: [idFamily])
}